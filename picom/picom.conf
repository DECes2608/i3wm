backend = "glx";
vsync = true;

# Sadece kitty'e blur, her şeye değil
blur: {
  method = "dual_kawase";
  strength = 5;
}
blur-background-exclude = [
  "class_g != 'kitty'"
];

corner-radius = 0;
shadow = false;
fading = false;
